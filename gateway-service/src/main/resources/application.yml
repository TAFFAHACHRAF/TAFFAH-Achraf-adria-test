server:
  port: 8888
spring:
  application:
    name: gateway-service
  #cloud:
    #gateway:
      #routes:
        #- id: r1
          #uri: http://localhost:8082/
          #predicates:
            #- Path=/customers/**
        #- id: r2
          #uri: http://localhost:8083/
          #predicates:
            #- Path=/products/**

